# C++ REST API - User Service

## Overview
A simple REST API built using Crow in C++ with layered architecture.

## Features
- Create User
- Get Users
- Get User by ID
- Delete User
- Thread-safe design

## Build & Run

```
mkdir build && cd build
cmake ..
make
./app
```

Server runs at: http://localhost:18080
